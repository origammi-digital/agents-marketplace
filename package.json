{
  "name": "@origammi/agents-marketplace",
  "version": "0.3.0",
  "description": "Origammi private agents marketplace — generic, context-aware AI agents for any project",
  "private": true,
  "type": "module",
  "bin": {
    "agents": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
